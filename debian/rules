#!/usr/bin/make -f
# You must remove unused comment lines for the released package.
#export DH_VERBOSE = 1

%:
	dh $@ --with python2

override_dh_auto_install:
	dh_atuo_install
	install -v -m0755 -D tools/changelog-gen $(DESTDIR)/usr/bin/changelog-gen
